# Regenerated by the sync scripts. The site only loads the .js files; the .json
# twins are convenience exports for other tooling, and committing both would
# double the fresh blobs written on every sync.
data/cards.json
data/prices.json

node_modules/
.DS_Store
Thumbs.db
