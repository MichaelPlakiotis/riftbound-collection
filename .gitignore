# Regenerated by sync-cards.mjs. The site only loads data/cards.js; cards.json is
# a convenience export, and committing both would add ~2.5 MB of new blobs on
# every sync instead of ~1.1 MB.
data/cards.json

node_modules/
.DS_Store
Thumbs.db
